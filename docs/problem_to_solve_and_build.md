# Escrowd — origin note

The product is **Escrowd**. The 6-hour build lives in **[plan.md](plan.md)**. Architecture: [Architecture.md](Architecture.md). Changelog: [changelog.md](changelog.md). Agents: `AGENTS.md`, `CLAUDE.md`, `cursor.md`, `grok.md` at the repo root.

This document was the first short pitch: Nour’s Instagram bio link, deposit then balance, live price, Arabic/RTL, webhook as truth. Everything here was folded into `plan.md` and corrected (order is created *before* checkout; price is recomputed on the server; HMAC is never skipped).

Do not implement against this file.
